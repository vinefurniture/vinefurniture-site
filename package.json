{
  "name": "vinefurniture-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "assets:check": "node scripts/check-vine-assets.mjs",
    "assets:manifest": "node scripts/generate-vine-asset-report.mjs --format=json",
    "assets:report": "node scripts/generate-vine-asset-report.mjs --format=markdown",
    "assets:intake": "node scripts/generate-vine-asset-intake.mjs --format=csv",
    "assets:intake:md": "node scripts/generate-vine-asset-intake.mjs --format=markdown",
    "preview:check": "node scripts/check-preview-readiness.mjs",
    "admin:validate": "node scripts/check-admin-publish-payload.mjs",
    "admin:import": "node scripts/import-admin-publish.mjs",
    "admin:extract-assets": "node scripts/extract-admin-draft-assets.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.2",
    "sass": "^1.100.0"
  }
}

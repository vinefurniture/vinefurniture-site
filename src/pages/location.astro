---
import Layout from '../layouts/Layout.astro';
import { pageContent, selectedAssets, siteMeta } from '../data/site';

const locationPage = pageContent.location;
---

<Layout
  title={locationPage.title}
  description={locationPage.description}
>
  <section class="section">
    <div class="container two-column">
      <div>
        <p class="eyebrow">{locationPage.eyebrow}</p>
        <h1>{locationPage.heroTitle}</h1>
        <p data-draft-field="locationPage.heroBody">{locationPage.heroBody}</p>
        <p class="note">최근 사진은 홈의 최근 사진 영역에서 먼저 확인하시고, 방문 전에는 이 페이지에서 위치와 운영시간만 빠르게 확인하시면 됩니다.</p>
        <div class="button-group">
          <a class="button" href={locationPage.primaryCtaHref} target="_blank" rel="noreferrer" data-draft-href="business.place" data-analytics-event="click_naver_place" data-analytics-label="location_hero_naver_place">{locationPage.primaryCtaLabel}</a>
          <a class="button secondary" href={locationPage.secondaryCtaHref} data-draft-href="business.phoneHref" data-analytics-event="click_call" data-analytics-label="location_hero_call">{locationPage.secondaryCtaLabel}</a>
          <a class="button secondary" href="/#recent-feed">최근 사진 보기</a>
        </div>
      </div>
      <div class="card-image-wrap hero-media-small">
        <img src={selectedAssets.storeView.src} alt={selectedAssets.storeView.alt} data-draft-image="storeView" />
        <div class="overlay-note">매장 위치 확인용 대표 이미지</div>
      </div>
    </div>
  </section>

  <section class="section" style="background: var(--color-surface);">
    <div class="container">
      <h2>{locationPage.memoTitle}</h2>
      <div class="grid-3">
        {locationPage.notes.map((item) => <article class="card"><h3>{item.title}</h3><p set:html={item.body.replace(/\n/g, '<br />')} /></article>)}
      </div>
    </div>
  </section>
</Layout>
